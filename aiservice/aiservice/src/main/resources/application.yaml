spring:
  application:
    name: ai-service

  mongodb:
    uri: mongodb://localhost:27017/fitnessrecommendation
    database: fitnessrecommendation

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        default-requeue-rejected: false   # 🔑 IMPORTANT

server:
  port: 8083

eureka:
  instance:
    hostname: localhost
    ip-address: 127.0.0.1
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking

gemini:
  api:
    url: ${GEMINI_URL}
    key: ${GEMINI_KEY}
